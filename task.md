## LAB 1：底层指令与运算时序 (The Core Logic)

**考核重点**：寻址方式、标志位（$CY, OV, AC, P$）、指令字节数与周期。

### TASK 1：五大寻址与数据搬运

- **实验内容**：编写汇编片段，将数据从一个地方转移到另一个地方。
  1. **直接寻址**：将 $40H$ 单元的内容传送到 $R0$。
  2. **间接寻址**：利用 $R1$ 作为指针，将内部 RAM $50H$ 地址的内容取出送入 $A$。
  3. **位寻址**：将 $20H.3$ 位（位地址 $03H$）的值取反送入 $P1.0$。
- **考试细节**：请写出 `MOV A, 40H` 和 `MOV A, #40H` 编译后分别占用几个字节？

### TASK 2：算术运算与 PSW 状态位

- **实验内容**：执行以下运算，并预测执行后 $PSW$ 寄存器的 $CY$（进位）和 $OV$（溢出）位。
  1. `MOV A, #0FFH`, `ADD A, #01H`（观察 $CY$）。
  2. `MOV A, #7FH`, `ADD A, #01H`（观察 $OV$，思考为什么补码运算溢出了？）。
  3. 使用 `MUL AB` 指令计算 $255 \times 2$，观察结果如何分布在 $A$ 和 $B$ 中。
- **考试细节**：$PSW$ 的 $P$ 位（奇偶校验）始终反映 $A$ 中 $1$ 的个数的奇偶性，它是硬件自动更新的。

### TASK 3：堆栈操作与 SP 指针

- **实验内容**：
  1. 执行 `PUSH ACC`，观察 $SP$ 值的变化（是先存后加，还是先加后存？）。
  2. 手动修改 $SP = 60H$，连续压入 3 个数据，观察数据存储的具体物理地址。
- **考试细节**：复位后 $SP = 07H$，因此第一个压栈的数据实际上存在 $08H$。

### TASK 4：循环控制与条件转移
* **实验内容**：利用判跳指令实现常见的逻辑结构。
    1.  **数据块搬运（单层循环）**：将内部 RAM 的 **40H~49H**（共 10 个字节）复制到 **50H~59H**。要求使用 **R0**、**R1** 作为地址指针，**R2** 作为循环计数器（赋初值 10），结合 `DJNZ` 指令实现。
    
    2.  **条件分支（寻找最大值）**：比较 **R3** 和 **R4** 的大小。如果 **R3 > R4**，将 **R3** 的值存入 **A**；否则将 **R4** 存入 **A**。提示：使用 `CJNE` 指令比较，并通过判断 **CY** 标志位来决定跳转。
    3.  **精准延时（嵌套循环）**：编写一个双重 `DJNZ` 嵌套循环子程序。
* **考试细节（填空/改错题陷阱）**：
    * `DJNZ` 指令的执行逻辑是**先减 1，再判断是否为 0**。如果给寄存器赋初值为 **0**，执行 `DJNZ` 时会先减成 **FFH** (即 255)，从而实际循环了 256 次。
    * `CJNE` 指令在比较不相等时发生跳转，同时相当于做了一次减法（目标操作数 - 源操作数）。如果 目标操作数 < 源操作数，硬件会自动将 **CY** 置 **1**，这是实现比较大小的唯一标准手段。

## LAB 2：C51 内存布局与编译器特性 (Language Specs)

**考核重点**：非冯·诺依曼架构下的 4 大存储空间、`sbit` 与寄存器定义。

### TASK 1：存储类型全解析

- **实验内容**：在同一个 C 文件里定义变量，并使用 `printf` 打印它们的地址或查看 `.m51` 文件。
  - `data`：低 128 字节 RAM（直接寻址，速度最快）。
  - `idata`：全部 256 字节 RAM（必须间接寻址）。
  - `pdata`：外部 RAM 的前 256 字节。
  - `xdata`：全部 64KB 外部存储空间。
- **考试细节**：为什么 `code` 类型的变量不能执行 `var = 10;` 操作？

### TASK 2：特殊功能寄存器 (SFR) 映射

- **实验内容**：
  1. 不使用 `reg51.h`，手动定义 `sfr P1 = 0x90;` 和 `sbit LED = P1^0;`。
  2. **考点**：所有的 SFR 地址都在 $80H \text{-} FFH$ 之间，且只有地址能被 $8$ 整除的寄存器才可以位寻址。

------

## LAB 3：定时器/计数器逻辑 (Hardware Config)

**考核重点**：$TMOD$、$TCON$、初值计算公式。

### TASK 1：定时器模式配置

- **实验内容**：
  1. 设置 $T0$ 为方式 1（16位定时器），$T1$ 为方式 2（8位自动重装载）。
  2. **写出 TMOD 的值**：注意 $TMOD$ 低 4 位给 $T0$，高 4 位给 $T1$。
- **考试细节**：$TMOD$ 是**不可位寻址**的，所以必须一次性赋值 `TMOD = 0x21;`。

### TASK 2：初值（计数初值）手算

- **题目**：晶振 $12\text{ MHz}$，要求定时 $50ms$。
- **计算过程**：
  1. 机器周期 $= 12 / (12 \times 10^6) = 1\mu s$。
  2. 所需计数值 $= 50ms / 1\mu s = 50000$。
  3. 初值 $X = 65536 - 50000 = 15536 = 3CB0H$。
  4. 代码写入：`TH0 = 0x3C; TL0 = 0xB0;`

------

## LAB 4：中断系统深度剖析 (System Integration)

**考核重点**：中断优先级、中断向量、现场保护。

### TASK 1：中断优先级与嵌套

- **实验内容**：
  1. 配置 $IP$ 寄存器，让串口中断的优先级高于定时器 0。
  2. **思考**：当两个优先级相同的中断同时申请时，谁先执行？（考察自然优先级顺序）。
- **考试细节**：记住自然优先级：外0 > 定0 > 外1 > 定1 > 串口。

### TASK 2：汇编 ISR 规范

- **实验内容**：编写一个完整的外部中断 0 处理程序。

  代码段

  ```
  ORG 0003H      ; 外部中断0入口
  LJMP EX0_ISR
  ...
  EX0_ISR:
      PUSH ACC
      PUSH PSW
      ; [执行你的逻辑]
      POP PSW
      POP ACC
      RETI       ; 关键：RETI会告知中断控制逻辑“当前中断已处理完”
  ```

------

## LAB 5：串口通信与波特率 (Communication)

**考核重点**：$SCON$、$SBUF$、波特率翻倍 ($SMOD$)。

### TASK 1：串口方式 1 配置

- **实验内容**：配置串口为 8 位异步通信，允许接收。
  1. 设置 `SCON = 0x50;`（$SM0=0, SM1=1, REN=1$）。
  2. **手算波特率初值**：$11.0592\text{ MHz}$ 下 $9600$ 波特率的 $TH1$ 是多少？（答案是 $FDH$）。

### TASK 2：SBUF 的物理本质

- **实验内容**：在 C 语言中执行 `SBUF = dat;` 和 `dat = SBUF;`。
- **考点**：$SBUF$ 在物理上是两个独立的寄存器：发送寄存器和接收寄存器，虽然它们共用一个地址 $99H$。

------

## LAB 6：综合应用设计 (Final Challenge)

**考核重点**：端口 I/O 结构、按键去抖、综合逻辑。

### TASK 1：P0 口的特殊性

- **实验内容**：尝试在仿真中直接用 $P0$ 口驱动 LED。
- **考点**：$P0$ 是漏极开路（Open-Drain），必须外接上拉电阻。

### TASK 2：矩阵键盘扫描算法

- **实验内容**：编写一个 $4 \times 4$ 键盘扫描程序，通过串口打印按下的键值。
- **逻辑要求**：包含软件消抖逻辑（延时 $10 \text{-} 20ms$ 再次判断）。

------

### 期中考前最后一分钟自测：

1. **$ALE$ 信号**：它的频率是晶振频率的多少？（答案：$1/6$）。
2. **复位状态**：复位后 $P0 \text{-} P3$ 口的状态是什么？（答案：全为高电平 $FFH$）。
3. **中断使能**：要开启串口中断，必须配置哪两个位？（答案：$ES = 1$ 和 $EA = 1$）。
4. **外部寻址**：当 $EA$ 引脚接高电平时，单片机从哪里开始执行程序？（答案：内部 Flash）。

最后回答你之前那个问题：在 C51 中要使用 `printf`，你需要手动实现 **`putchar`** 函数（在 `stdio.h` 中有原型），通常就是把数据塞进 `SBUF` 并等待 `TI` 置位。

这份详细版 LAB 够你应付绝大部分期中考题了。如果你在写 `CJNE` 跳转逻辑或者初值计算时卡住了，随时找我探讨！